<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssologin.mapper.UserMapper">
	<resultMap type="com.ssologin.pojo.User" id="cas_user">
		<id property="id" column="id"/>
		<result property="name" column="name_"/>
		<result property="firstName" column="first_"/>
		<result property="lastName" column="last_"/>
		<result property="email" column="email_"/>
		<result property="username" column="username"/>
		<result property="password" column="pwd_"/>
		<result property="companyId" column="company_id_"/>
		<result property="isAdmin" column="is_admin_"/>
		<result property="idDelete" column="is_delete_"/>
	
	</resultMap>
	
	<!-- 根据用户名查找 -->
	<select id="findByName" resultMap="cas_user">
		select * from sys_user where username = #{username};
	</select>
	
	<select id="findById" resultMap="cas_user">
		select * from sys_user where id = #{id};
	</select>
	<!-- 添加 -->
	<insert id="addUser" parameterType="User">
		insert into sys_user(id,username,pwd_,first_,last_,email_,company_id_,name_) 
		value (#{id},#{username},#{password},#{firstName},#{lastName},#{email},#{companyId},#{name});
	</insert>
	<!-- 删除 -->
	<update id="deleteUserById" parameterType="String">
		update sys_user set is_delete_ = 1 where id = #{id};
	</update>
	<!-- 修改 -->
	<update id="updateUserById" parameterType="User">
	update sys_user
	set username = #{username},pwd_ = #{password},name_ = #{name},first_ = #{firstName},last_ =
	#{lastName}
	where id = #{id};
	</update>
	<!-- 查询所有 -->
	<select id="findAllUsers" resultMap="cas_user">
		select * from sys_user where is_delete_ != 1;
	</select>
</mapper>